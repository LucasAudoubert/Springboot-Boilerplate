package com.example.demo.repository;

/**
 * Marker package — Spring Data's JpaRepository is imported directly
 * from org.springframework.data.jpa.repository.JpaRepository in concrete
 * repository interfaces. Do not define a class/interface named JpaRepository
 * in this package: it shadows the Spring type and breaks component scanning.
 */
